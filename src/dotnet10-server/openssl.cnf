# WTT_TLS_GROUP must be set before process startup; container default is P-256.
openssl_conf = default_conf

[default_conf]
ssl_conf = ssl_sect

[ssl_sect]
system_default = system_default_sect

[system_default_sect]
Groups = $ENV::WTT_TLS_GROUP

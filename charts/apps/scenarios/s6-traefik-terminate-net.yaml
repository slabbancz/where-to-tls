routing:
  mode: terminate
  gatewayClass: traefik
  backendPort: 8443
  certificateSecretName: wtt-tls-secret
workload-go:
  enabled: false
workload-net:
  enabled: true
  labels:
    scenario: s6-traefik-terminate-net
    tls: traefik
  service:
    type: ClusterIP
  config:
    tls:
      enabled: true
      resumption: true
